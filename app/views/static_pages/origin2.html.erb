<%# Function needed for when logged in to show log out button and welcome text. %>
<%# Why isn't this in react? %>
<% if logged_in? %> 
  <script id="bootstrap-current-user" type="text/javascript">
  	window.currentUser = <%= render("api/users/user.json.jbuilder",
  		user: current_user).html_safe %>
  </script>
<% end %> %>

<main id="origin">Kapiteh Times View</main>


<% if logged_in? %> 
  <% logged_in_user = current_user %> 
<% else %> 
  <% logged_in_user = [] %> 
<% end %>

<script id="bootstrap-current-user" type="text/javascript">
  window.currentUser = 
  <%= render("api/users/user.json.jbuilder", user: logged_in_user).html_safe %>
</script>